{# <div class="col-md-6">
    <div class="card mx-2 mb-2">
        <img src="{{ project.data.image }}" alt="">
        <div class="card-body">
            <h5 class="card-title">{{ project.data.title }}</h5>
            <p class="card-text">{{ project.data.summary }}</p>
            <a href=" {{ project.data.siteUrl}}" class="btn btn-primary"> View Project</a>
        </div>
    </div>
</div> #}

<div class="col-md-4 p-3 border">
  <img src="./images/test.png" alt="" class="img-fluid mx-auto d-block border">
  <p class="lead p-3 pt-5 border">
    {{ project.data.title }} <br>
    {{ project.data.summary }} <br>
    <a href="{{ project.data.siteUrl}}" class="btn btn-primary"> View Project</a>
  </p>
</div>

